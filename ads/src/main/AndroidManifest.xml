<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions cần cho Google Mobile Ads. Khai báo ở đây để module tự đủ
         (manifest merger dedupe nếu host app đã có). App ID của AdMob
         (com.google.android.gms.ads.APPLICATION_ID) thuộc về host app vì là
         giá trị riêng từng app — xem :app AndroidManifest. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
</manifest>
