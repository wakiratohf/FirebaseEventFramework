# Google Mobile Ads SDK ships its own consumer ProGuard rules via its AAR,
# nên module này không cần keep rule riêng. Giữ file để khớp convention các
# module library khác (firebase-events, app-event).
