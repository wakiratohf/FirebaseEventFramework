# Disable auto-add of kotlin-stdlib to `api` configuration. lintPublish only
# accepts exactly one jar in the published configuration — any transitive
# runtime dep (incl. stdlib) breaks `:firebase-events:prepareLintJarForPublish`.
kotlin.stdlib.default.dependency=false
