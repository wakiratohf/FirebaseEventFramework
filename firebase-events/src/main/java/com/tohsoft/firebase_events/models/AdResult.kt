package com.tohsoft.firebase_events.models

/**
 * String constants for [ShowAdEv.result]. Mirror of the constants previously
 * imported from `com.tohsoft.ads.models.AdResult`; mirrored here so the SDK
 * does not depend on any ads module.
 */
object AdResult {
    const val SUCCESS = "success"
    const val FAILED = "failed"
}
